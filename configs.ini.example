[SERIAL]
; Set port to auto, demo, or the path (e.g., /dev/ttyAMA0).
port = demo

[TOTAL STATION]
make = Topcon
model = GTS-303

[BACKSIGHT ERROR]
; Acceptable error range for backsight shots (expected horizontal distance vs. measured distance), in cm.
limit = 3
